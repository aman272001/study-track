import CrudPage from "../components/CrudPage";

export default function Tasks() { return <CrudPage type="tasks" title="Tasks" fields={[{ name: "title", label: "Title", required: true }, { name: "description", label: "Description" }, { name: "deadline", label: "Deadline", type: "date" }, { name: "priority", label: "Priority", options: ["Low", "Medium", "High"] }]} columns={[{ key: "title", label: "Title" }, { key: "deadline", label: "Deadline", render: (item) => item.deadline ? new Date(item.deadline).toLocaleDateString() : "-" }, { key: "priority", label: "Priority", render: (item) => <span className={`badge ${item.priority?.toLowerCase()}`}>{item.priority || "Medium"}</span> }, { key: "completed", label: "Status", render: (item) => <span className={`badge ${item.completed ? "done" : ""}`}>{item.completed ? "Completed" : "Pending"}</span> }]} />; }